include ../icon/icon

mixin title(options)
    -
        var options = options ? options : {};
        var {text = "title", icon = ""} = options;
    header.title&attributes(attributes)
        .title__outer
            .title__icon
                +icon({type: "icon", image: icon})
            h1.title__text= text
            hr.title__decorline